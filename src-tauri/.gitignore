# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Built llama-server sidecar binaries (produced by
# scripts/build-llama-server.sh; build.rs writes placeholders for dev/CI).
# The version pin is tracked; the (large, platform-specific) binary is not.
/binaries/*
!/binaries/LLAMA_CPP_VERSION
